version: 2
models:
  - name: stg_claims
    description: "Staging claims table"
    columns:
      - name: claim_id
        tests:
          - not_null
          - unique
      - name: claim_amount
        tests:
          - not_null
      - name: claim_status
        tests:
          - not_null